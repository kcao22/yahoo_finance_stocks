star:
  target: prod
  outputs:
    prod:
      type: bigquery
      method: service-account
      project: "{{ env_var('AIRFLOW_VAR_GCP_PROJECT_ID') }}"
      dataset: dbt
      threads: 1
      keyfile: "{{ env_var('AIRFLOW_VAR_GCP_KEY_PATH')}}"
